# Database Connection
# For local development, use your local PostgreSQL connection string
# For Railway, use the DATABASE_URL provided by Railway's PostgreSQL service
DATABASE_URL="postgresql://user:password@localhost:5432/incident_tracking?schema=public"
